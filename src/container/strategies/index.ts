import { BaseStrategy } from './base.strategy';
import { DockerStrategy } from './docker.strategy';
import { SwarmStrategy } from './swarm.strategy';
import { KubernetesStrategy } from './kubernetes.strategy';

export { BaseStrategy, DockerStrategy, SwarmStrategy, KubernetesStrategy };
